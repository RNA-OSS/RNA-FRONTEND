import React from "react";

import * as Styled from "./style";

const Table = ({ headData, bodyData }) => {
    let renderTableContents = (bodyData) => tableData.map((
        <React.Fragment>

        </React.Fragment>
    ))
  return (
    <Styled.Table>
      <thead>
        <tr></tr>
      </thead>
      <tbody></tbody>
    </Styled.Table>
  );
};

export default Table;
